# Mobile (этап 2)

Мобильное приложение (Android/iOS) подключается к тому же REST + WebSocket API, что и админ-панель:

- список агентов / статусы
- push-уведомления по `agent_events`
- быстрые команды → `POST /api/commands`

Рекомендуемый стек на следующем шаге: **React Native** или **Flutter**.
